{
  "name": "pdf-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Portable PDF toolkit for merging multiple PDFs and Markdown to PDF conversion.",
  "author": "Tunahan Balci <tunahan.balci@example.com>",
  "main": "main.js",
  "dependencies": {
    "html2canvas": "^1.4.1",
    "jspdf": "^4.2.1",
    "marked": "^18.0.5",
    "pdf-lib": "^1.17.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3",
    "vite": "^5.3.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "electron:dev": "vite & sleep 2 && electron . --no-sandbox",
    "electron:build": "vite build && electron-builder",
    "electron:build:linux": "vite build && electron-builder --linux",
    "electron:build:win": "vite build && electron-builder --win"
  },
  "build": {
    "appId": "com.pdftools.app",
    "productName": "pdf-tools",
    "directories": {
      "output": "dist_electron"
    },
    "files": [
      "dist/**/*",
      "main.js",
      "preload.js",
      "package.json"
    ],
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Office",
      "icon": "public/logo512.png"
    },
    "win": {
      "target": [
        "portable"
      ],
      "icon": "public/logo512.png"
    }
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
